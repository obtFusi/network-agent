# Development dependencies (installed AFTER requirements.txt)
# Do NOT use -r requirements.txt here — it uses --require-hashes
# which forces ALL packages to have hashes. Install separately:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

# Testing
pytest>=8.0
pytest-cov>=4.0

# Linting & Formatting
ruff>=0.8.0
