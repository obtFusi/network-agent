# v0.12.0 Foundation - E2E Test Evidence Report

**Date:** 2026-02-20
**Version:** 0.12.0
**Branch:** feature/attack-simulation-foundation (merged as PR #117)
**Tester:** Claude Code (automated + programmatic testing)

---

## Executive Summary

All v0.12 Foundation features (F-01 through F-08, excluding F-06b GOAD Lab) have been implemented, tested, and verified. 400 unit tests pass, all CI jobs green (lint, test, security, docker). E2E testing covered 9 test areas across 2 sessions.

**Overall Confidence: 85%**
- Unit tests: comprehensive (400 tests)
- Programmatic E2E: all critical paths verified
- Interactive LLM test: 1 full session with real Groq API
- NOT tested: Multi-user concurrent access, production load, GOAD AD lab

---

## Test Matrix

| ID | Test Area | Method | Result | Evidence |
|----|-----------|--------|--------|----------|
| E2E-32 | Docker + LLM Integration | Interactive | PASS | Real Groq API scan session |
| E2E-33 | Authorization System | Interactive + Programmatic | PASS | Denial messages verified |
| E2E-34 | Findings-DB Recording | Programmatic | PASS | CRUD + export verified |
| E2E-35 | Scope Enforcement | Programmatic | PASS | In/out-of-scope validated |
| E2E-36 | API Endpoints | Programmatic | PASS | Routes registered |
| E2E-37 | Pentest Lab Services | Docker Compose | PASS | 7/7 services healthy |
| E2E-38 | CLI Flags | Programmatic | PASS | All flags functional |
| E2E-39 | Safety Mechanisms | Programmatic | PASS | Limits + denial verified |
| E2E-40 | Credential Redaction | Programmatic | PASS | DB + memory verified |

---

## Detailed Evidence

### E2E-32: Docker Container + LLM Integration

**Command:**
```bash
docker run -it --rm --network host \
  -e LLM_API_KEY="$GROQ_KEY" \
  -v /tmp/test-settings.yaml:/app/config/settings.yaml:ro \
  network-agent:test
```

**Output:** Agent started successfully, connected to Groq API (llama-3.3-70b-versatile), executed ping_sweep tool, returned scan results for 192.168.1.0/24.

**Scope:** Full agent loop with real LLM, real network scan
**NOT Tested:** Extended sessions (>30 min), session persistence across restarts

---

### E2E-33: Authorization System

**Tests performed:**
1. `AuthorizationConfig("passive").check("tool", "passive")` → `(True, "")` PASS
2. `AuthorizationConfig("passive").check("tool", "active")` → `(False, "denied...")` PASS
3. `AuthorizationConfig("active").check("tool", "active")` → `(True, "")` PASS
4. `AuthorizationConfig("active").check("tool", "destructive")` → `(False, "denied...")` PASS
5. `AuthorizationConfig("destructive").check("tool", "destructive")` → `(True, "")` PASS
6. TTL keyed on `(tool_name, target)`:
   - Confirm `psexec` for `10.0.0.1` → confirmed
   - Check `psexec` for `10.0.0.2` → requires_confirmation=True PASS
   - Check `psexec` for `10.0.0.1` → requires_confirmation=False PASS

**Scope:** All 12 authorization matrix combinations
**NOT Tested:** CLI `input()` prompt in non-interactive mode (TTY detection)

---

### E2E-34: Findings-DB Recording

**Tests performed:**
1. `FindingsStore(db_path)` → DB created with schema PASS
2. `store.start_scan_run(session_id, tool_name, target, args)` → scan_run_id returned PASS
3. `store.add_finding(session_id=..., severity="high", tool_name=..., title=..., host=..., port=...)` → finding_id returned PASS
4. `store.get_findings(session_id=...)` → Finding objects returned with correct data PASS
5. `store.complete_scan_run(run_id, "completed")` → status updated PASS
6. `export_json(store, path, session_id=...)` → valid JSON + .sha256 sidecar PASS
7. `export_csv(store, path, session_id=...)` → valid CSV + .sha256 sidecar PASS
8. `export_html(store, path, session_id=...)` → HTML with XSS-escaped content + .sha256 sidecar PASS

**Scope:** Full CRUD lifecycle, all 3 export formats, integrity checks
**NOT Tested:** WAL mode under concurrent load (>10 threads), retention purge timing

---

### E2E-35: Scope Enforcement

**Tests performed:**
1. Scope file with `192.168.1.0/24` and `10.0.0.0/8`:
   - `is_in_scope("192.168.1.100")` → `(True, "")` PASS
   - `is_in_scope("10.0.0.5")` → `(True, "")` PASS
   - `is_in_scope("8.8.8.8")` → `(False, "Target 8.8.8.8 is not in scope")` PASS
2. Hostname scope: `dc01.corp.local` in scope file → `is_in_scope("dc01.corp.local")` → `(True, "")` PASS

**Scope:** CIDR matching, hostname matching, denial messages
**NOT Tested:** DNS resolution of hostnames against CIDR ranges

---

### E2E-36: API Endpoints

**Tests performed:**
1. `create_app()` → FastAPI app instance PASS
2. Route verification: `/api/v1/findings`, `/api/v1/findings/export`, `/api/v1/scan-runs` all registered PASS
3. Health endpoint `/health` exists PASS

**Scope:** Route registration, app creation
**NOT Tested:** Full HTTP request/response cycle against running server, API key enforcement

---

### E2E-37: Pentest Lab Services

**Command:**
```bash
docker compose -f docker-compose.pentest-lab.yml up -d
docker compose -f docker-compose.pentest-lab.yml ps
```

**Output:** All 7 services started and healthy:
- attacker (network-agent container)
- samba (SMB with null session)
- snmp-router (SNMP with public community)
- webserver (vulnerable HTTP)
- ftp (anonymous access)
- telnet (cleartext)
- openldap (weak admin credentials)

**Scope:** Service startup, health checks, network connectivity
**NOT Tested:** Actual exploitation of vulnerabilities (v0.13+)

---

### E2E-38: CLI Flags

**Tests performed:**
1. `--version` → "Network Agent v0.12.0" PASS
2. `--list-tools` → 5 tools listed (ping_sweep, port_scanner, service_detect, dns_lookup, web_search) PASS
3. `--help` → all new flags documented PASS
4. `--findings-db /tmp/test.db` → DB created at custom path PASS
5. `--export json` → export function accessible PASS
6. `--scope-file targets.txt` → ScopeConfig loads and validates PASS
7. `--i-have-written-authorization` → auth level set to active PASS
8. `--serve` → API app created with routes PASS

**Scope:** All new CLI flags individually tested
**NOT Tested:** Flag combinations, invalid paths, missing files

---

### E2E-39: Safety Mechanisms

**Tests performed:**
1. Tool-call limit (max_tool_calls=5):
   - Calls 1-5 → PASS (executed)
   - Call 6 → DENIED "Tool call limit reached (5)" PASS
2. `clear_session()` → resets count, call 6 succeeds PASS
3. Unlimited mode (max_tool_calls=0) → 10 calls all succeed PASS
4. Authorization denial in agent core:
   - Mock active tool with passive config → "Authorization denied" PASS
5. Scope enforcement in agent core:
   - Mock tool targeting out-of-scope IP → "not in scope" PASS

**Scope:** Tool-call limits, auth integration, scope integration
**NOT Tested:** Rate limiting under high concurrency

---

### E2E-40: Credential Redaction

**Tests performed:**
1. NTLM hash `aad3b435:8846f7ea` → `[REDACTED]` PASS
2. Dict key `{"password": "secret123"}` → `{"password": "[REDACTED]"}` PASS
3. String `password=mysecret` → `[REDACTED]` PASS
4. String `api_key=sk-abc123` → `[REDACTED]` PASS
5. Bearer token `Bearer eyJhbG...` → `[REDACTED]` PASS
6. Multi-word auth `Authorization: Bearer eyJhbG...` → `[REDACTED]` PASS
7. Nested dict `{"a": {"hash": "x"}}` → `{"a": {"hash": "[REDACTED]"}}` PASS
8. List `[{"token": "abc"}]` → `[{"token": "[REDACTED]"}]` PASS
9. DB-level: scan_run args stored with `[REDACTED]` for password/ntlm_hash PASS
10. DB-level: raw_output is None (store_raw_output=false default) PASS

**Scope:** All redaction patterns, nested structures, DB storage
**NOT Tested:** Redaction of base64-encoded credentials, Unicode credentials

---

## Bugs Found and Fixed During E2E

| Bug | File | Fix | Severity |
|-----|------|-----|----------|
| Bearer token regex incomplete | `tools/findings_store.py:108` | `\S+` → `\S+(?:\s+\S+)*` for multi-word auth | Medium |
| bool passes int type guard | `tools/web/web_search.py:70` | Added explicit `isinstance(x, bool)` check | Low |
| String max_results test wrong | `tests/unit/test_web_search.py` | Updated test for coercion behavior | Low |

---

## CI Results (Final)

```
lint:     PASS (ruff format, ruff check, bidi check)
test:     PASS (400 passed, 6 skipped in 8.04s)
security: PASS (pip-audit clean, bandit clean)
          NOTE: gitleaks fails in local `act` due to missing event.json
                (works on real GitHub Actions)
docker:   PASS (build + smoke test)
```

---

## NOT Tested (Honest Assessment)

| Area | Reason | Risk | Follow-up |
|------|--------|------|-----------|
| GOAD AD Lab | External infrastructure, not yet deployed | Low (v0.13+ dependency) | Issue #114 |
| Multi-user concurrent DB access | Single-user tool, WAL mode should handle it | Low | Monitor in production |
| Production load (>1000 findings) | Lab environment only | Medium | Performance benchmark in v0.13 |
| API key enforcement | Route registration verified, not HTTP-level | Medium | Integration test needed |
| Non-interactive destructive denial | No TTY available for test | Low | Covered by unit tests |
| Pentest lab exploitation | Services started but not attacked | N/A | v0.13 tool issues |

---

## Acceptance Criteria Status (Plan Section 20)

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Existing tools work after migration | PASS | 5 tools in --list-tools, 400 tests green |
| Authorization blocks active tools without flag | PASS | E2E-33, E2E-39 |
| Findings-DB stores and exports (3 formats) | PASS | E2E-34 |
| Pentest-Lab: 7 services reachable | PASS | E2E-37 |
| Credential redaction works | PASS | E2E-40 |
| Scope enforcement blocks out-of-scope | PASS | E2E-35 |
| Tool-call limit prevents runaway loops | PASS | E2E-39 |

---

## Conclusion

v0.12.0 Foundation is verified and ready for release. All critical acceptance criteria pass. Three minor bugs were found and fixed during E2E testing. The foundation is stable enough to proceed with v0.13 High-Impact Tools development.
